NAVRYNT repository metadata update

Copy README.md, LICENSE and banner.png into the ROOT of the navrynt-website repository.

Then run:

git add README.md LICENSE banner.png
git commit -m "docs: add banner and Apache 2.0 license"
git push origin main

The existing GitHub Pages workflow will redeploy automatically.
